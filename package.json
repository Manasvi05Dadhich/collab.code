{
  "dependencies": {
    "express": "^5.2.1",
    "http-server": "^14.1.1",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3",
    "ws": "^8.20.0",
    "y-websocket": "^1.5.4",
    "yjs": "^13.6.30"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "scripts": {
    "server:dev": "nodemon server.js",
    "server:prod": "node server.js",
    "install:client": "cd collab.code && npm install",
    "build:client": "cd collab.code && CI=false npm run build",
    "build": "npm run install:client && npm run build:client",
    "start": "node server.js"
  }
}
